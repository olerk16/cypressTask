{
  "name": "simple-cypress-example",
  "version": "1.0.0",
  "description": "project to demonstrate Cypress tests with GitHub Actions",
  "main": "index.js",
  "scripts": {
    "start": "npx http-server src -p 8080",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^12.17.3",
    "http-server": "^14.1.1",
    "wait-on": "^7.0.1"
  }
}
